---
interface Props {
  class?: string;
  dataTestId?: string;
}
const { class: className = "", dataTestId } = Astro.props;
---

<article
  data-testid={dataTestId}
  class:list={[
    className,
    "rounded-xl p-3 transition-all duration-200 ease-out",
  ]}
>
  <slot />
</article>
